spring:
  profiles: eureka1
  application:
    name: eureka-server
server:
  port: 8088
eureka:
  instance:
    hostname: localhost
  client:
#    register-with-eureka: false
#    fetch-registry: false
#    service-url.defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    service-url.defaultZone: http://${eureka.instance.hostname}:8089/eureka/